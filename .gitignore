# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated AppSumo redemption codes — never commit live codes
appsumo-codes-*.csv

# Local environment. This repo is public, so nothing from here belongs in it —
# the two values it holds today are public by design, but the next one might not be.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
